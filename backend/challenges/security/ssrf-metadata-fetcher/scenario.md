# Scenario

The platform includes a "URL preview" utility used by support engineers. It fetches remote content server-side and returns a summary to the browser.

## Goal

Identify SSRF risk in URL handling and propose controls that prevent internal network abuse.
